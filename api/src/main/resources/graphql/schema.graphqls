scalar Long

type Query {
    classrooms: [Classroom]
    classroom(id: ID!): Classroom
}

type Classroom {
    id: ID!
    no: String!
    name: String!
    userNum: Long!
    createdAt: Long!
    updatedAt: Long!
}